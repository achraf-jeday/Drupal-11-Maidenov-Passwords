# Development override configuration
# This file will be automatically loaded by Docker Compose
# Use this for development-only services

version: '3.8'

services:
  # Uncomment the services you want to use during development

  # MailHog for email testing
  # mailhog:
  #   image: mailhog/mailhog:latest
  #   ports:
  #     - "1025:1025"  # SMTP
  #     - "8025:8025"  # Web UI
  #   restart: unless-stopped

  # Redis Commander for Redis management (if you enable Redis)
  # redis-commander:
  #   image: rediscommander/redis-commander:latest
  #   ports:
  #     - "8082:8081"
  #   environment:
  #     REDIS_HOSTS: local:redis:6379
  #   depends_on:
  #     - redis
  #   restart: unless-stopped

  # Nginx reverse proxy (optional)
  # nginx:
  #   image: nginx:alpine
  #   ports:
  #     - "80:80"
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf:ro
  #   depends_on:
  #     - drupal
  #   restart: unless-stopped
